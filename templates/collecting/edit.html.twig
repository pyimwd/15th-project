{% extends 'base.html.twig' %}

{% block title %}Update
	{{ collecting.name }}
{% endblock %}

{% block body %}
	<container>
		<div class="row mt-4 mb-4">
			<h1>Update
				{{ collecting.name }}</h1>
		</div>

		<hr>

		{{ form_start(updateCollectingForm)}}
		{{ form_row(updateCollectingForm.name)}}

		<div class="row mt-4 mb-4">
			<div class="col text-start">
				{{ include('collecting/_delete_form.html.twig') }}
			</div>
			<div class="col text-end">
				<button class="btn btn-secondary">
					<a href="{{ path('collecting') }}" class="text-decoration-none text-white">Cancel</a>
				</button>
				<button type="submit" class="btn btn-success">
					Update
				</button>
			</div>

		</div>


		{# {{ include('collecting/_form.html.twig', {'button_label': 'Update'}) }} #}

		{# <a href="{{ path('collecting') }}">back to list</a> #}

	</container>
	{{ form_end(updateCollectingForm)}}
{% endblock %}
